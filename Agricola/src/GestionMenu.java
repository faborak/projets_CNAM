import java.awt.event.*;public class GestionMenu implements ActionListener {private int n;		public GestionMenu(int n){	this.n = n;}public void actionPerformed(ActionEvent e){	switch (n) {	case 1 : // Nouvelle partie, non definit dans ce projet		break;	case 2 : // Pas de sauvegarde dans cette version		break;	case 3 : // Pas de sauvegarde dans cette version		break;	case 4 : System.exit(0);	break;	case 5 : // L'ajout de la fenetre de regle n'est pas faite dans le cadre du projet		break;	default :		break;}	}}