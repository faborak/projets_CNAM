'use strict';

describe('myApp.followswap module', function() {

  beforeEach(module('myApp.followswap'));

  describe('followswap controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var followswapCtrl = $controller('followswapListCtrl');
      expect(followswapCtrl).toBeDefined();
    }));

  });
});